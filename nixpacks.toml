# nixpacks.toml — Railway build config
# Installs Python (for yt-dlp) + ffmpeg alongside Node.js

[phases.setup]
nixPkgs = ["python311", "ffmpeg", "nodejs_20"]

[phases.install]
cmds = ["npm install", "pip install yt-dlp curl-cffi"]

[start]
cmd = "node server/index.js"
